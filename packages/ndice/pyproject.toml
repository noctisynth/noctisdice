[project]
name = "ndice"
version = "0.1.0-rc.1"
description = "夜骰基础掷骰指令规则包"
authors = [
    {name = "苏向夜",email = "fu050409@163.com"},
]
dependencies = [
    "infini>2.1.0",
    "diceutils>=0.3.6",
]
requires-python = ">=3.8"
readme = "README.md"
license = {text = "AGPLv3"}

[tool.pdm]
distribution = true

[tool.pdm.dev-dependencies]
dev = ["pytest"]
